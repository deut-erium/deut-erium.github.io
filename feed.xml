---
layout: null
permalink: /feed.xml
sitemap: false
---
<?xml version="1.0" encoding="utf-8"?>
<feed xmlns="http://www.w3.org/2005/Atom"{% if site.lang %} xml:lang="{{ site.lang | xml_escape }}"{% endif %}>
  <generator uri="https://jekyllrb.com/" version="{{ jekyll.version }}">Jekyll</generator>
  <title type="html">{{ site.title | smartify | xml_escape }}</title>
  <subtitle>{{ site.description | xml_escape }}</subtitle>
  <link href="{{ page.url | absolute_url }}" rel="self" type="application/atom+xml" />
  <link href="{{ '/' | absolute_url }}" rel="alternate" type="text/html" />
  <id>{{ page.url | absolute_url | xml_escape }}</id>
  <updated>{{ site.time | date_to_xmlschema }}</updated>
  <author><name>{{ site.author.name | default: site.author | xml_escape }}</name>{% if site.author.email %}<email>{{ site.author.email | xml_escape }}</email>{% endif %}{% if site.author.uri %}<uri>{{ site.author.uri | xml_escape }}</uri>{% endif %}</author>
  {%- comment -%}Use the archive's order, including ties. Sorting by date again reverses tied entries.{%- endcomment -%}
  {%- assign _feed_posts = site.posts -%}
  {%- unless site.show_drafts -%}{%- assign _feed_posts = _feed_posts | where_exp: 'post', 'post.draft != true' -%}{%- endunless -%}
  {%- assign _feed_limit = site.feed.posts_limit | default: 10 -%}
  {%- for _post in _feed_posts limit: _feed_limit %}
  <entry{% if _post.lang %} xml:lang="{{ _post.lang | xml_escape }}"{% endif %}>
    {%- assign _title = _post.title | smartify | strip_html | normalize_whitespace -%}
    <title type="html">{{ _title | xml_escape }}</title>
    <link href="{{ _post.url | absolute_url }}" rel="alternate" type="text/html" title="{{ _title | xml_escape }}" />
    <id>{{ _post.id | absolute_url | xml_escape }}</id>
    <published>{{ _post.date | date_to_xmlschema }}</published>
    <updated>{{ _post.last_modified_at | default: _post.date | date_to_xmlschema }}</updated>
    {%- assign _author = _post.author | default: _post.authors[0] | default: site.author -%}
    {%- assign _author = site.data.authors[_author] | default: _author -%}
    <author><name>{{ _author.name | default: _author | xml_escape }}</name>{% if _author.email %}<email>{{ _author.email | xml_escape }}</email>{% endif %}{% if _author.uri %}<uri>{{ _author.uri | xml_escape }}</uri>{% endif %}</author>
    {%- if _post.category %}<category term="{{ _post.category | xml_escape }}" />{% else %}{% for _category in _post.categories %}<category term="{{ _category | xml_escape }}" />{% endfor %}{% endif -%}
    {%- for _tag in _post.tags %}<category term="{{ _tag | xml_escape }}" />{% endfor -%}
    {%- assign _summary = _post.description | default: _post.excerpt | strip_html | normalize_whitespace -%}
    <summary type="text">{{ _summary | xml_escape }}</summary>
  </entry>
  {%- endfor %}
</feed>
