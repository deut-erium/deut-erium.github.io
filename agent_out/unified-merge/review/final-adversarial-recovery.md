# CONFIRMED

The second catalog recovery fix works at HEAD `87f3de8bdac521ab9d9505042c2370611d51789b`. I did not rely on the retained focused matrix: its `sourceCommit` is `bcd9f02`, so its provenance does not establish current HEAD. The source and `_site-next` copies of `catalog.js`, `index.html`, and `catalog.css` are byte-for-byte equal; the source JavaScript and HTML Git blob IDs also match HEAD.

Checked:

- With the HTTP cache disabled, blocking the catalog entry module produced one inspector-blocked resource load. The [entry fallback](https://github.com/deut-erium/deut-erium.github.io/blob/87f3de8bdac521ab9d9505042c2370611d51789b/new-tetris/src/catalog/index.html#L121-L132) exposed the full "could not start" message, kept the machine hidden, exposed zero machine controls, selected no size, and left `aria-busy` absent.
- Blocking only the initial `data-4.js?v=20260911` import allowed `catalog.js` and `catalog-model.js` to run first, then exposed the same startup message. The machine stayed hidden with zero families, placeholder totals, no selected size, and no residual `aria-busy`.
- I replaced the default 4x4 state with a stronger preservation case before blocking `data-6.js`: filter `I2`, family `I2-J2`, step 2 of 4, open text arrangement, 24 total families, 117 layouts, and three visible matches. A SHA-256 snapshot covering the machine DOM except the two intended failure-status nodes, live form values, scroll positions, URL, and canvas pixels was identical before and after failure: state `6c59e93f9ab35392d189bec59ad549545f515a30b50d91fa64a7db1f9428a88f`; canvas `41315f014ceef9a9d097c0277520874f7685faeff3e22fb8664bcdc607f3b586`. Size 4 and `I2-J2` remained selected, and `aria-busy` was removed by the [failure path](https://github.com/deut-erium/deut-erium.github.io/blob/87f3de8bdac521ab9d9505042c2370611d51789b/new-tetris/src/catalog/catalog.js#L344-L397).
- The [visible failure status](https://github.com/deut-erium/deut-erium.github.io/blob/87f3de8bdac521ab9d9505042c2370611d51789b/new-tetris/src/catalog/index.html#L27-L32) rendered as a 1180 by 57 pixel block with the complete retry instruction. Chrome's accessibility tree exposed it as a non-ignored `status` with `live=polite`, `atomic=true`, and `relevant="additions text"`. Its text exactly matched the separate [polite live status](https://github.com/deut-erium/deut-erium.github.io/blob/87f3de8bdac521ab9d9505042c2370611d51789b/new-tetris/src/catalog/index.html#L91-L94).
- After unblocking, a second 6x6 selection in the same document retained the injected document token, kept one navigation performance entry, and caused no frame navigation. The blocked URL was `data-6.js?v=20260911`; the retry code requested exactly one fresh `data-6.js?v=20260911&retry=1` URL. It returned HTTP 200 as `text/javascript`, with neither disk-cache nor service-worker fulfillment. The recovered page selected 6x6 and `O9`, reported `1,467` total and visible families, and contained 1,467 buttons with 1,467 unique family IDs. The error was cleared and `aria-busy` was absent.
- Pre-document listeners recorded zero unhandled rejections in every scenario, and CDP recorded zero runtime exceptions. The blocked entry emitted only its expected resource error; each caught data-import failure emitted its deliberate console error. Recovery emitted no console error or failed load. All page requests stayed on the loopback origin.
