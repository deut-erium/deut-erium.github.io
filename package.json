{
  "name": "deuterium-writeups-build",
  "private": true,
  "version": "1.0.0",
  "description": "Build-only tools for the static WriteUps site",
  "scripts": {
    "sync-katex-assets": "python3 script/prune-katex-css.py"
  },
  "devDependencies": {
    "katex": "0.18.1"
  }
}
